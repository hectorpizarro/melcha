/**
 * Maximo length permitido dentro de input
 */
export const INPUT_MAX_LEN = 100;

/**
 * Estoy usando 'available_filters' para obtener las categorias. La lista puede ser muy larga, encima de los 25 items. Para hacer mas legible la pagina estoy mostrando el top 5 de las categorias con mas results.
 */
export const TOP_CAT_LEN = 5;

/**
 * Este es un ejemplo de auth token, el real deberia retornarse despues de login y almacenarse en el rootReducer. Se envia en todos los API requests para recibir Author data.
 */
export const AUTH_TOKEN = "qwerty";
